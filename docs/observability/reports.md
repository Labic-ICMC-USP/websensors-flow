# Local reports

Every run writes two files:

```text
execution_report.json
execution_report.md
```

The report contains pipeline metadata, step order, status, duration, metrics, parameters, artifacts, warnings, and captured failures.
