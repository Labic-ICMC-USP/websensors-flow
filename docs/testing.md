# Testing

Run the test suite with:

```bash
pytest
```

The tests use memory observers and local settings. External Graylog and MLflow services are not required unless a test explicitly enables those observers.
